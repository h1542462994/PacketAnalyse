<Window x:Class="PacketAnalyse.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PacketAnalyse"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <Button x:Name="ButtonContiune" Width="64" Content="开始" Margin="5" Click="Button_Click_Contiune"/>
            <Button x:Name="ButtonPause" Width="64" IsEnabled="False" Content="暂停" Margin="5" Click="Button_Click_Pause"/>
            <Button Width="64" Content="清空记录" Margin="5" Click="Button_Click_Clear"/>
        </StackPanel>
        <DataGrid x:Name="DataGridMain" ItemsSource="{Binding}" Grid.Row="1" AutoGenerateColumns="True">
            
        </DataGrid>
    </Grid>
</Window>
